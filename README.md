# video-to-shorts

A pipeline to automate the conversion of YouTube videos to shorts.

## Folder Structure
- `stages/` : Contains the pipeline stages.
- `output/` : Auto-created at runtime for generated clips.
